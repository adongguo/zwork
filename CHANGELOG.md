# 更新日志

## 1.52.0 - 2026-09-15

1.51.0 不会检查更新，需要手动安装一次 1.52.0，之后的版本可以在设置里更新。macOS 版支持 Apple Silicon。

### 新增

- 逻辑工作区与会话归档、文件和差异标签、Files 与 Changes 侧栏，以及把对话和文档排成滚动列的画布布局。
- 工作区没有打开的标签时，直接显示可输入的对话框，发送后才创建对话。
- `⌘⇧T` 在原位置重新打开最近关闭的标签，包括和对话一起关闭的文件与差异标签。标签栏下方的提示提供“重新打开”和“查看归档”。
- 已开始的对话可以切换模型。切换前会确认：新模型要重新读取整个对话，token 消耗会增加。Claude Code、Qoder CLI、千问办公、Gemini CLI 等无法接管已有历史的 Provider，在对话有消息后不能切换过去。
- 正式版从 GitHub Releases 检查更新，并在后台下载；可以在设置里关闭“自动下载更新”。只安装使用 ZWork Developer ID 签名并经 Apple 公证的更新，在设置 → App 点击“重启以更新”完成替换。
- 选择 Provider 时自动发现模型。
- 工作项状态和类型筛选支持多选，可以用开关隐藏 Automation。

### 变更

- `⌘W` 关闭当前标签，`⌘⇧W` 关闭窗口，`⌘T` 直接打开新对话标签。
- “窗口置顶”的默认快捷键改为 `⌥⌘T`。
- 新对话默认使用工作区的默认目录，没有时使用该工作区最近一次对话的目录。
- 关闭标签不再弹出“撤销”提示，关闭的对话保留在归档里。
- CLI Provider 在切换模型和思考强度时保留进程，流式显示思考过程和排队状态，取消后及时停止。
- 只列出本机可以运行的 Provider。
- 隐藏本地推理、外部后端和 Provider 认证设置。
- 优化设计 token、对话活动行和输入框。

### 修复

- 看板隐藏筛选之外的状态泳道。
- 保留未发送的草稿；对话控制按钮移到标题栏下方，不再被窗口拖动区域挡住。

## 1.51.0 - 2026-09-13

ZWork 首个正式版，基于 goose 1.50.0，macOS 版支持 Apple Silicon。

### 新增

- 云效个人工作台：工作项看板与列表、代码库与本地克隆、代码与差异、合并请求、流水线和制品库。各模块可以在设置里关闭，工作项和合并请求可以直接交给 Agent 处理。
- Qoder CLI 与千问办公 Provider。千问办公通过内置 sidecar 复用 QwenWorkCN 桌面端的登录态。
- 新对话可以在对话页直接切换 Provider 和模型，只列出本机可用的 Provider，对话开始后锁定。
- 按工作目录分组的 workspace 标签，重启后自动恢复；侧栏支持会话搜索和状态筛选。
- 命令面板和会话快速跳转，快捷键可以自定义。
- Dracula PRO 主题：Dracula Pro、Alucard、Blade、Buffy、Lincoln、Morbius、Van Helsing，以及 Aura。
- 桌面伙伴 XiYang：跟随对话状态，支持视线跟随、单击、双击、长按和拖拽互动，空闲时会打盹。
- 通过 `zwork desktop` 命令行和 `zwork-desktop` skill 操作正在运行的应用。
- 合并请求参与人和工作项负责人显示 Codeup 头像，工作项卡片按阶段、类型和优先级着色。

### 变更

- 采用 ZWork 品牌、图标和字体，默认语言为英文。
- 更新源、遥测、文档站等上游在线服务改为按需开启，默认关闭。

### 修复

- 暗色主题下设置开关看不清。
